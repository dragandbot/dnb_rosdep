sympy-pip:
  debian:
    pip:
      packages: [sympy]
  ubuntu:
    pip:
      packages: [sympy]
scipy-pip:
  debian:
    pip:
      packages: [scipy]
  ubuntu:
    pip:
      packages: [scipy]
imutils-pip:
  debian:
    pip:
      packages: [imutils]
  ubuntu:
    pip:
      packages: [imutils]
scikit-optimize-pip:
  debian:
    pip:
      packages: [scikit-optimize]
  ubuntu:
    pip:
      packages: [scikit-optimize]
