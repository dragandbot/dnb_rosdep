sympy-pip:
  debian:
    pip:
      packages: [sympy]
  ubuntu:
    pip:
      packages: [sympy]
scipy-pip:
  debian:
    pip:
      packages: [scipy]
  ubuntu:
    pip:
      packages: [scipy]
scikit-optimize-pip:
  debian:
    pip:
      packages: [scikit-optimize]
  ubuntu:
    pip:
      packages: [scikit-optimize]
